This parser reads a backup.ATracker file that can be created in the time-tracking app ATracker and creates a compact pandas dataframe with the most valuable information from the time logs.
The DataFrame contains columns Name, Tag, start-time and end-time
